<li>
  <%= image_tag "rails.png", alt: "Avatar" %>
  <%= link_to user.name, user %>
  <% if current_user.admin? and not current_user?(user) %>
    | <%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>

